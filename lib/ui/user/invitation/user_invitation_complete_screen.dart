import 'package:flutter/material.dart';

class UserInvitationCompleteScreen extends StatelessWidget {
  const UserInvitationCompleteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
